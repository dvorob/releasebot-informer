{{ $conf := .Values.ymreleasebot }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $conf.census.name }}
spec:
  ports:
  - name: {{ $conf.census.name }}
    port: {{ $conf.census.port }}
    protocol: TCP
    targetPort: {{ $conf.census.port }}
  selector:
    app: {{ $conf.census.name }}
  type: ClusterIP